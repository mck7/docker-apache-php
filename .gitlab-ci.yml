image: docker:latest

services:
  - docker:dind

stages:
  - build

build:
  stage: build
  script:
    - docker login --username="$DOCKER_USER" --password="$DOCKER_PASS"
    - docker build --rm -t corycollier/apache-php:$CI_BUILD_REF_SLUG .
    - docker push corycollier/apache-php:$CI_BUILD_REF_SLUG
